decl a
a=0

decl b
b=0

setsprite(29, 8)
setcol(0, 2, 0)

while ( b <= 5 ) {

    while ( a <= 60 ) {
        a = a + 1
        setpos( a, a )
    }

    while ( a > 0 ) {
        a = a - 1
        setpos( a, a )
    }
}

